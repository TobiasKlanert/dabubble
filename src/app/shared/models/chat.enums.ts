export enum ChatType {
  DirectMessage = 'chats',
  Channel = 'channels',
  NewChat = 'newChat',
  None = 'none'
}

export enum SearchType {
  AddUser,
  ShowProfile,
  MentionUserOrChannel,
  NewChat
}
